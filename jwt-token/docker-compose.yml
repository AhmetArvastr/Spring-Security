version: '3.8'

services:
  db:
    image: mysql
    restart: always
    container_name: my-db
    environment:
      MYSQL_ROOT_PASSWORD: myroot
      MYSQL_DATABASE: my-db
      MYSQL_USER: my
      MYSQL_PASSWORD: 1234asdf
    ports:
      - '3306:3306'